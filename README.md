# DSS Brindavana Site

Minimal Vite + React project that embeds a YouTube video and links the provided Google Maps address.

Run locally:

1. npm install
2. npm run dev

The site is modeled visually from https://savithridevelopers.com/ per request.
